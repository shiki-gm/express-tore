<div class="weatherWidget">
    {{!-- #each 可以拿到res.locals中的变量，前提是经过app.use --}}
    {{#each partials.weatherContext}}
        <div class="location">
            <h3>{{location.name}}</h3>
            <a href="{{forecastUrl}}">
                <img src="{{iconUrl}}" alt="weather" />
                {{weather}}, {{temp}}
            </a>
        </div>
    {{/each}}
    <small>Source: <a href="https://www.npmjs.com/package/express-handlebars">National Weather Service</a></small>
</div>